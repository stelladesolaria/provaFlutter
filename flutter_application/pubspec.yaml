name: app_abastecimento
description: App de controle de abastecimento e veículos - trabalho
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.6.0
  firebase_auth: ^4.4.0
  cloud_firestore: ^4.5.0
  provider: ^6.0.5
  intl: ^0.18.0
  flutter_material_pickers: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/

